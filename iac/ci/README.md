This folder contains CI workflow templates for running the test suite.

To enable the workflow on GitHub, copy `ci.yml` to `.github/workflows/`:

```bash
mkdir -p .github/workflows
cp iac/ci/ci.yml .github/workflows/ci.yml
```
